// const obj = { width: 10, height: 15 };
// const area = obj.width * obj.heigth;

// console.log(area);

const add = (a, b) => {
    return a + b;
}

console.log(add(3, "drew"));

const point = {x: 50, y: 700, z: 600}